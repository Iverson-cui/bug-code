#include<stdio.h>

int main(void){
	char b[] = { '0','2'};
	char a[] = { 0 };
	
	printf("now a is %d,%d", a[0], a[1]);
	copy(a, b);
	printf("now a is %d %d", a[0], a[1]);
	return 0;
}

char* copy(char* a, char* b) {
	char* t = b;
	while (*a++ = *b++);//b is copied into a
	return t;
}
